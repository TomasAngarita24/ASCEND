// VAPID application server key (public). It must match the backend's
// VAPID_PUBLIC_KEY; the key is public by design, so baking it here lets the
// frontend subscribe to push without extra build-time configuration.
export const VAPID_APPLICATION_SERVER_KEY =
  'BEab38ineZbv4NjAHwEPNMy4VY2IkElOWPonFMghpnjziQIstvQNRnoFiyJaMNw-UR-ohcbVz_i_uNpiUJQXCUc';