DATABASE_URL="postgresql://ascend:ascend_dev_password@localhost:5432/ascend?schema=public"
JWT_ACCESS_SECRET="replace-with-a-random-secret-of-at-least-32-characters"
JWT_ISSUER="ascend-api"
JWT_AUDIENCE="ascend-app"
# Comma-separated list of allowed browser origins (CORS).
CORS_ORIGINS="http://localhost:5173,http://127.0.0.1:5173"

# --- Email (password reset) ---
# Resend API key. Without it, reset links are only logged to the console.
# RESEND_API_KEY="re_..."
# Sender used by Resend. With the free plan it must stay as onboarding@resend.dev.
# With a verified custom domain, use "ASCEND <noreply@tudominio.com>".
# RESEND_FROM_EMAIL="ASCEND <onboarding@resend.dev>"

# --- Google Sign-In (Web/PWA; hides the button when absent) ---
# GOOGLE_CLIENT_ID="xxxx.apps.googleusercontent.com"
