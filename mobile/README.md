# Mobile (React Native + TypeScript)

Initial scaffold for the ASCEND mobile application.

The rest-timer feature is local to the mobile application. It does not require a backend endpoint and provides start, pause, resume, skip, and duration-adjustment controls.
